name: action_sheet
version: 1.0.2
description: A material bottom sheet with clickable actions as seen in Google Drive.
repository: https://github.com/FelixBoehme/action_sheet/

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
    
flutter:
  uses-material-design: true